# API base URL. Used by the frontend to call the backend.
# Local (Mac) development: backend runs on port 8000.
# AWS deployment (same-origin): leave empty so requests go to the same host (e.g. proxy /api or /boarding to backend). See docs/DEPLOYMENT.md.
NEXT_PUBLIC_API_URL=http://localhost:8000
